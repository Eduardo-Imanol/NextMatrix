export interface Lesson {
  topicId: string;
  title: string;
  sections: { heading: string; content: string }[];
}

export const reactLessons: Lesson[] = [
  {
    topicId: 'react-basics',
    title: 'React Basico',
    sections: [
      { heading: 'Que es React?', content: 'React es una biblioteca de JavaScript creada por Meta (Facebook) para construir interfaces de usuario. Se enfoca en un solo aspecto: la capa de vista (UI). Ventajas principales:\n\n- **Componentes**: La UI se divide en piezas reutilizables.\n- **Virtual DOM**: Actualizaciones eficientes sin recargar la pagina.\n- **Declarativo**: Describes QUE quieres ver, no COMO construirlo.\n- **Gran ecosistema**: React Router, Redux, Next.js, etc.' },
      { heading: 'Virtual DOM', content: 'React crea una copia ligera del DOM real en memoria (Virtual DOM). Cuando el estado cambia:\n\n1. React crea un nuevo Virtual DOM.\n2. Compara con el anterior (diffing).\n3. Solo actualiza los nodos que cambiaron en el DOM real.\n\nEsto es mucho mas rapido que manipular el DOM directamente, ya que el DOM real es costoso.' },
      { heading: 'Componentes', content: 'Los componentes son piezas reutilizables de UI. Hay dos tipos:\n\n**Componentes funcionales** (recomendados):\nfunction MiComponente() {\n  return <h1>Hola, mundo</h1>;\n}\n\n**Componentes de clase** (antiguos):\nclass MiComponente extends React.Component {\n  render() {\n    return <h1>Hola, mundo</h1>;\n  }\n}\n\nUsa componentes funcionales. Son mas simples y modernos.' },
      { heading: 'JSX', content: 'JSX es una extension de sintaxis que parece HTML pero vive en JavaScript:\n\nfunction App() {\n  const nombre = "Ana";\n  return (\n    <div>\n      <h1>Hola, {nombre}</h1>\n      <p>Mi edad es: {25 + 5}</p>\n    </div>\n  );\n}\n\nReglas de JSX:\n- Una sola etiqueta raiz (usa <>...</> para fragmentos).\n- Los atributos usan camelCase (className, onClick).\n- Las expresiones van entre {}.\n- Las etiquetas deben cerrarse (img />' },
      { heading: 'Rendering', content: 'Para mostrar un componente en la pantalla:\n\nimport React from "react";\nimport ReactDOM from "react-dom/client";\nimport App from "./App";\n\nconst root = ReactDOM.createRoot(\n  document.getElementById("root")\n);\n\nroot.render(\n  <React.StrictMode>\n    <App />\n  </React.StrictMode>\n);\n\nStrictMode ayuda a detectar problemas en desarrollo.' },
    ]
  },
  {
    topicId: 'react-jsx',
    title: 'JSX en Profundidad',
    sections: [
      { heading: 'Expresiones en JSX', content: 'JSX permite cualquier expresion JavaScript entre {}:\n\nfunction App() {\n  const nombre = "Ana";\n  const hour = new Date().getHours();\n  const saludo = hour < 12 ? "Buenos dias" : "Buenas tardes";\n  \n  return (\n    <div>\n      <p>Hola, {nombre}</p>\n      <p>{saludo}</p>\n      <p>2 + 2 = {2 + 2}</p>\n      <p>{nombre.toUpperCase()}</p>\n    </div>\n  );\n}\n\nNo puedes usar if/else directamente, pero si ternarios o &&.' },
      { heading: 'Renderizado condicional', content: 'function App({ isLoggedIn }) {\n  return (\n    <div>\n      {/* Ternario */}\n      {isLoggedIn ? <p>Bienvenido</p> : <p>Inicia sesion</p>}\n      \n      {/* AND logico */}\n      {isLoggedIn && <p>Tu perfil</p>}\n      \n      {/* Variable */}\n      {isLoggedIn && (\n        <div>\n          <h2>Dashboard</h2>\n        </div>\n      )}\n    </div>\n  );\n}\n\nEvita usar if antes del return. Prefiere ternarios para eleccion entre dos cosas y && para mostrar algo o nada.' },
      { heading: 'Listas y keys', content: 'Para renderizar arrays de elementos, usa .map():\n\nfunction App() {\n  const frutas = ["Manzana", "Pera", "Uva"];\n  \n  return (\n    <ul>\n      {frutas.map((fruta, index) => (\n        <li key={index}>{fruta}</li>\n      ))}\n    </ul>\n  );\n}\n\n**La prop key es obligatoria**. React la usa para identificar cada elemento y optimizar las actualizaciones. Usa IDs unicos, no indices.' },
      { heading: 'Estilos en JSX', content: 'Tres formas de aplicar estilos:\n\n**1. className (recomendado):**\n<div className="card activo"> contenido </div>\n\n**2. Style en linea (objeto):**\n<div style={{ color: "red", fontSize: "20px" }}>\n  Texto rojo\n</div>\nNota: las propiedades van en camelCase.\n\n**3. CSS Modules:**\nimport styles from "./App.module.css";\n<div className={styles.card}> contenido </div>' },
      { heading: 'Eventos en JSX', content: 'Los eventos usan camelCase:\n\nfunction App() {\n  const handleClick = () => {\n    alert("Clic!");\n  };\n  \n  const handleSubmit = (e) => {\n    e.preventDefault();\n    console.log("Formulario enviado");\n  };\n  \n  return (\n    <div>\n      <button onClick={handleClick}>Clic aqui</button>\n      \n      <form onSubmit={handleSubmit}>\n        <input type="text" />\n        <button type="submit">Enviar</button>\n      </form>\n    </div>\n  );\n}\n\nPasa la funcion, no la llamada: onClick={handleClick}, no onClick={handleClick()}.' },
    ]
  },
  {
    topicId: 'react-functional-components',
    title: 'Componentes Funcionales',
    sections: [
      { heading: 'Anatomia de un componente', content: 'Un componente funcional es una funcion que retorna JSX:\n\nfunction Saludo({ nombre }) {\n  return (\n    <div>\n      <h1>Hola, {nombre}</h1>\n      <p>Bienvenido a la aplicacion</p>\n    </div>\n  );\n}\n\n// Uso\n<Saludo nombre="Ana" />\n\nReglas:\n- El nombre debe empezar con mayuscula.\n- Debe retornar JSX (o null).\n- Un componente = una responsabilidad.' },
      { heading: 'Composicion de componentes', content: 'Los componentes se ensamblan como piezas de LEGO:\n\nfunction Tarjeta({ titulo, children }) {\n  return (\n    <div className="tarjeta">\n      <h2>{titulo}</h2>\n      <div className="contenido">\n        {children}\n      </div>\n    </div>\n  );\n}\n\nfunction App() {\n  return (\n    <Tarjeta titulo="Mi Tarjeta">\n      <p>Este contenido va como children</p>\n      <button>Clic</button>\n    </Tarjeta>\n  );\n}\n\nchildren es una prop especial que contiene el contenido entre las etiquetas del componente.' },
      { heading: 'Componentes como archivos', content: 'En proyectos reales, cada componente va en su propio archivo:\n\n**Archivo: components/Boton.jsx**\nexport function Boton({ texto, onClick }) {\n  return (\n    <button className="btn" onClick={onClick}>\n      {texto}\n    </button>\n  );\n}\n\n**Archivo: App.jsx**\nimport { Boton } from "./components/Boton";\n\nfunction App() {\n  return (\n    <Boton texto="Clic" onClick={() => alert("Hola")} />\n  );\n}\n\nCrea una carpeta components/ para organizarlos.' },
      { heading: 'Patrones comunes', content: '**Presentational vs Container:**\n- Presentational: Solo muestra UI (sin logica).\n- Container: Maneja logica y datos.\n\n**Render props:**\nfunction MouseTracker({ render }) {\n  const [pos, setPos] = useState({ x: 0, y: 0 });\n  return render(pos);\n}\n\n// Uso\n<MouseTracker render={(pos) => <p>Mouse: {pos.x}, {pos.y}</p>} />\n\n**Compound components:**\n<Tabs>\n  <Tabs.List>\n    <Tabs.Tab>Pestana 1</Tabs.Tab>\n    <Tabs.Tab>Pestana 2</Tabs.Tab>\n  </Tabs.List>\n  <Tabs.Panel>Contenido 1</Tabs.Panel>\n  <Tabs.Panel>Contenido 2</Tabs.Panel>\n</Tabs>' },
    ]
  },
  {
    topicId: 'react-props-state',
    title: 'Props y State',
    sections: [
      { heading: 'Que son las Props?', content: 'Las props son datos que un componente recibe de su padre. Son de solo lectura (inmutables):\n\nfunction Usuario({ nombre, edad, email }) {\n  return (\n    <div>\n      <h2>{nombre}</h2>\n      <p>Edad: {edad}</p>\n      <p>Email: {email}</p>\n    </div>\n  );\n}\n\n// Uso\n<Usuario nombre="Ana" edad={25} email="a@b.com" />\n\n- Los strings se pasan sin comillas: nombre="Ana"\n- Los numeros, objetos y funciones van en {}: edad={25}\n- Las props fluyen de padre a hijo (unidireccional).' },
      { heading: 'Props por defecto', content: 'function Boton({ texto = "Clic", variante = "primario", onClick }) {\n  return (\n    <button className={"btn-" + variante} onClick={onClick}>\n      {texto}\n    </button>\n  );\n}\n\n// Uso\n<Boton /> {/* texto="Clic", variante="primario" */}\n<Boton texto="Enviar" /> {/* variante="primario" */}\n<Boton variante="secundario" /> {/* texto="Clic" */}\n\nO con desestructuracion:\nfunction Boton(props) {\n  const { texto = "Clic", variante = "primario" } = props;\n  // ...\n}' },
      { heading: 'Estado con useState', content: 'El estado es datos que el componente maneja internamente y que pueden cambiar:\n\nimport { useState } from "react";\n\nfunction Contador() {\n  const [count, setCount] = useState(0);\n  \n  return (\n    <div>\n      <p>Contador: {count}</p>\n      <button onClick={() => setCount(count + 1)}>+</button>\n      <button onClick={() => setCount(count - 1)}>-</button>\n    </div>\n  );\n}\n\n- useState(0): Valor inicial del estado.\n- [count, setCount]: Array desestructurado.\n- setCount: Unica forma de modificar el estado.\n- **Nunca** mutues el estado directamente: count++ esta mal.' },
      { heading: 'Estado con objetos', content: 'function Formulario() {\n  const [formData, setFormData] = useState({\n    nombre: "",\n    email: "",\n    edad: 0\n  });\n  \n  const handleChange = (e) => {\n    const { name, value } = e.target;\n    setFormData(prev => ({\n      ...prev,\n      [name]: value\n    }));\n  };\n  \n  return (\n    <form>\n      <input name="nombre" value={formData.nombre} onChange={handleChange} />\n      <input name="email" value={formData.email} onChange={handleChange} />\n    </form>\n  );\n}\n\nSiempre copia el estado anterior con spread operator (...prev) antes de modificar.' },
      { heading: 'Lifting state up', content: 'Cuando dos componentes necesitan compartir estado, subelo al padre mas cercano:\n\nfunction Padre() {\n  const [temperatura, setTemperatura] = useState(20);\n  \n  return (\n    <div>\n      <Termometro temperatura={temperatura} />\n      <Control temperatura={temperatura} onChange={setTemperatura} />\n    </div>\n  );\n}\n\nfunction Termometro({ temperatura }) {\n  return <p>Temperatura: {temperatura}C</p>;\n}\n\nfunction Control({ temperatura, onChange }) {\n  return (\n    <button onClick={() => onChange(temperatura + 1)}>\n      +1C\n    </button>\n  );\n}\n\nEl estado vive en el padre y se pasa por props.' },
    ]
  },
  {
    topicId: 'react-events-forms',
    title: 'Eventos y Formularios',
    sections: [
      { heading: 'Manejo de eventos', content: 'React usa eventos similares a los del DOM, pero en camelCase:\n\nfunction Botones() {\n  const handleClick = (e) => {\n    e.preventDefault();\n    console.log("Clic en:", e.target);\n  };\n  \n  const handleKeyDown = (e) => {\n    if (e.key === "Enter") {\n      console.log("Enter presionado");\n    }\n  };\n  \n  return (\n    <div>\n      <button onClick={handleClick}>Clic</button>\n      <input onKeyDown={handleKeyDown} />\n    </div>\n  );\n}\n\nEventos comunes: onClick, onChange, onSubmit, onKeyDown, onFocus, onBlur.' },
      { heading: 'Formularios controlados', content: 'Un formulario controlado vincula el valor del input al estado:\n\nfunction Formulario() {\n  const [nombre, setNombre] = useState("");\n  const [email, setEmail] = useState("");\n  \n  const handleSubmit = (e) => {\n    e.preventDefault();\n    console.log({ nombre, email });\n  };\n  \n  return (\n    <form onSubmit={handleSubmit}>\n      <input\n        type="text"\n        value={nombre}\n        onChange={(e) => setNombre(e.target.value)}\n        placeholder="Nombre"\n      />\n      <input\n        type="email"\n        value={email}\n        onChange={(e) => setEmail(e.target.value)}\n        placeholder="Email"\n      />\n      <button type="submit">Enviar</button>\n    </form>\n  );\n}\n\nVentajas: validacion instantanea, formato dinamico, control total.' },
      { heading: 'Multiples inputs', content: 'Un solo handler para varios inputs:\n\nfunction Formulario() {\n  const [datos, setDatos] = useState({\n    nombre: "",\n    email: "",\n    mensaje: ""\n  });\n  \n  const handleChange = (e) => {\n    setDatos({\n      ...datos,\n      [e.target.name]: e.target.value\n    });\n  };\n  \n  return (\n    <form>\n      <input name="nombre" value={datos.nombre} onChange={handleChange} />\n      <input name="email" value={datos.email} onChange={handleChange} />\n      <textarea name="mensaje" value={datos.mensaje} onChange={handleChange} />\n    </form>\n  );\n}\n\nLa clave es usar e.target.name para identificar que campo cambio.' },
      { heading: 'Select y otros controles', content: '**Select:**\nfunction PaisSelector() {\n  const [pais, setPais] = useState("mx");\n  \n  return (\n    <select value={pais} onChange={(e) => setPais(e.target.value)}>\n      <option value="mx">Mexico</option>\n      <option value="co">Colombia</option>\n      <option value="ar">Argentina</option>\n    </select>\n  );\n}\n\n**Checkbox:**\n<input\n  type="checkbox"\n  checked={acepto}\n  onChange={(e) => setAcepto(e.target.checked)}\n/>\n\n**Radio:**\n<input type="radio" name="color" value="rojo" checked={color === "rojo"} onChange={(e) => setColor(e.target.value)} />' },
    ]
  },
  {
    topicId: 'react-basic-hooks',
    title: 'Hooks Basicos: useState y useEffect',
    sections: [
      { heading: 'Que son los Hooks?', content: 'Los hooks son funciones que permiten "engancharte" a caracteristicas de React desde componentes funcionales. Reglas:\n\n1. Solo llamelos en el **nivel superior** (nunca dentro de loops, condiciones o funciones).\n2. Solo llamelos en **componentes funcionales**.\n\nHooks comunes: useState, useEffect, useContext, useRef, useMemo, useCallback.' },
      { heading: 'useState en detalle', content: 'const [estado, setEstado] = useState(valorInicial);\n\n**Valor inicial puede ser:**\nconst [count, setCount] = useState(0); // numero\nconst [nombre, setNombre] = useState(""); // string\nconst [items, setItems] = useState([]); // array\nconst [user, setUser] = useState(null); // objeto o null\n\n**Funcion lazy initialization:**\nconst [datos, setDatos] = useState(() => {\n  // Solo se ejecuta una vez\n  return JSON.parse(localStorage.getItem("datos")) || [];\n});\n\n**Actualizacion funcional:**\nsetCount(prev => prev + 1); // Usa el valor anterior' },
      { heading: 'useEffect', content: 'Ejecuta efectos secundarios despues del renderizado:\n\nimport { useEffect } from "react";\n\nfunction Reloj() {\n  const [hora, setHora] = useState(new Date());\n  \n  useEffect(() => {\n    const intervalo = setInterval(() => {\n      setHora(new Date());\n    }, 1000);\n    \n    // Funcion de limpieza\n    return () => clearInterval(intervalo);\n  }, []); // Array vacio = solo una vez\n  \n  return <p>{hora.toLocaleTimeString()}</p>;\n}\n\n**Dependencias:**\n- []: Se ejecuta solo al montar.\n- [dep1, dep2]: Se ejecuta cuando cambian esas dependencias.\n- Sin array: Se ejecuta en cada render.' },
      { heading: 'useEffect: Cleanup', content: 'La funcion de limpieza evita memory leaks:\n\nfunction Suscripcion() {\n  const [datos, setDatos] = useState(null);\n  \n  useEffect(() => {\n    const controller = new AbortController();\n    \n    fetch("/api/datos", { signal: controller.signal })\n      .then(r => r.json())\n      .then(setDatos);\n    \n    // Limpieza al desmontar o re-ejecutar\n    return () => controller.abort();\n  }, []);\n  \n  return <div>{datos?.map(d => <p key={d.id}>{d.nombre}</p>)}</div>;\n}\n\nSiempre limpia: intervalos, suscripciones, abort controllers, event listeners.' },
      { heading: 'Patrones comunes', content: '**Fetching de datos:**\nfunction Usuarios() {\n  const [usuarios, setUsuarios] = useState([]);\n  const [cargando, setCargando] = useState(true);\n  \n  useEffect(() => {\n    fetch("/api/usuarios")\n      .then(r => r.json())\n      .then(data => {\n        setUsuarios(data);\n        setCargando(false);\n      });\n  }, []);\n  \n  if (cargando) return <p>Cargando...</p>;\n  return usuarios.map(u => <p key={u.id}>{u.nombre}</p>);\n}\n\n**localStorage:**\nconst [tema, setTema] = useState(() => {\n  return localStorage.getItem("tema") || "claro";\n});\n\nuseEffect(() => {\n  localStorage.setItem("tema", tema);\n}, [tema]);' },
    ]
  },
  {
    topicId: 'react-conditional-rendering',
    title: 'Renderizado Condicional y Listas',
    sections: [
      { heading: 'Renderizado condicional', content: 'Muestra diferentes elementos segun condiciones:\n\nfunction App({ usuario, estaLogueado }) {\n  // Opcion 1: Ternario\n  return estaLogueado ? <Dashboard usuario={usuario} /> : <Login />;\n  \n  // Opcion 2: && logico\n  return (\n    <div>\n      <h1>Inicio</h1>\n      {estaLogueado && <p>Bienvenido, {usuario.nombre}</p>}\n    </div>\n  );\n  \n  // Opcion 3: Variable\n  let contenido;\n  if (estaLogueado) {\n    contenido = <Dashboard />;\n  } else {\n    contenido = <Login />;\n  }\n  return <div>{contenido}</div>;\n}' },
      { heading: 'Renderizado de listas', content: 'Convierte arrays en elementos JSX:\n\nfunction ListaTareas() {\n  const [tareas, setTareas] = useState([\n    { id: 1, texto: "Estudiar React", completada: false },\n    { id: 2, texto: "Hacer ejercicios", completada: true },\n    { id: 3, texto: "Practicar", completada: false }\n  ]);\n  \n  return (\n    <ul>\n      {tareas.map(tarea => (\n        <li key={tarea.id} style={{\n          textDecoration: tarea.completada ? "line-through" : "none"\n        }}>\n          {tarea.texto}\n        </li>\n      ))}\n    </ul>\n  );\n}\n\nSiempre usa key con un valor unico (ID, no indice).' },
      { heading: 'Filtrar y transformar', content: 'Combina .filter() con .map():\n\nfunction FiltroTareas() {\n  const [filtro, setFiltro] = useState("todas");\n  const [tareas, setTareas] = useState([...]);\n  \n  const tareasFiltradas = tareas.filter(tarea => {\n    if (filtro === "completadas") return tarea.completada;\n    if (filtro === "pendientes") return !tarea.completada;\n    return true; // "todas"\n  });\n  \n  return (\n    <div>\n      <select value={filtro} onChange={(e) => setFiltro(e.target.value)}>\n        <option value="todas">Todas</option>\n        <option value="completadas">Completadas</option>\n        <option value="pendientes">Pendientes</option>\n      </select>\n      \n      {tareasFiltradas.map(tarea => (\n        <Tarea key={tarea.id} {...tarea} />\n      ))}\n    </div>\n  );\n}' },
      { heading: 'Indices como key (cuando)', content: 'Usa el indice como key **solo** cuando:\n- La lista es estatica (no cambia orden, no se agrega/elimina).\n- No tienes un ID unico.\n\n// OK: lista estatica\nconst colores = ["rojo", "azul", "verde"];\ncolores.map((color, index) => <span key={index}>{color}</span>)\n\n// MAL: lista dinamica\n// Nunca uses index como key si la lista puede cambiar\n\n**Problema**: si se reordena, React reutiliza el componente incorrectamente, causando bugs visuales.' },
    ]
  },
  {
    topicId: 'react-lifecycle',
    title: 'Ciclo de Vida con Hooks',
    sections: [
      { heading: 'Fases del ciclo de vida', content: 'Todo componente pasa por tres fases:\n\n1. **Montaje**: El componente se crea y se agrega al DOM.\n2. **Actualizacion**: El componente se re-renderiza (por cambio de props o estado).\n3. **Desmontaje**: El componente se elimina del DOM.\n\nCon hooks, estas fases se manejan con useEffect.' },
      { heading: 'Montaje', content: 'Ejecuta codigo cuando el componente se monta:\n\nfunction App() {\n  useEffect(() => {\n    console.log("Componente montado");\n    // Equivale a componentDidMount\n    \n    // Carga inicial de datos\n    fetch("/api/datos")\n      .then(r => r.json())\n      .then(setDatos);\n  }, []); // [] = solo una vez\n}\n\nEn el montaje:\n- Carga datos iniciales.\n- Suscribirse a servicios.\n- Configurar intervalos/event listeners.' },
      { heading: 'Actualizacion', content: 'Ejecuta codigo cuando las dependencias cambian:\n\nfunction Usuario({ userId }) {\n  const [usuario, setUsuario] = useState(null);\n  \n  useEffect(() => {\n    fetch("/api/users/" + userId)\n      .then(r => r.json())\n      .then(setUsuario);\n  }, [userId]); // Se ejecuta cuando userId cambia\n  \n  return usuario ? <p>{usuario.nombre}</p> : <p>Cargando...</p>;\n}\n\n**Cada valor en el array de dependencias** se compara con Object.is(). Si cambia, se ejecuta el efecto.' },
      { heading: 'Desmontaje', content: 'La funcion de limpieza se ejecuta al desmontar:\n\nfunction Temporizador() {\n  const [segundos, setSegundos] = useState(0);\n  \n  useEffect(() => {\n    const intervalo = setInterval(() => {\n      setSegundos(s => s + 1);\n    }, 1000);\n    \n    // Limpieza al desmontar\n    return () => {\n      console.log("Limpiando intervalo");\n      clearInterval(intervalo);\n    };\n  }, []);\n  \n  return <p>{segundos} segundos</p>;\n}\n\nSin cleanup, el intervalo seguiria ejecutandose despues de que el componente desaparezca.' },
      { heading: 'Patron: useEffect con cleanup', content: 'Ejemplo completo de un componente con suscripcion:\n\nfunction ChatRoom({ roomId }) {\n  const [mensajes, setMensajes] = useState([]);\n  \n  useEffect(() => {\n    const conexion = crearConexion(roomId);\n    \n    conexion.on("mensaje", (msg) => {\n      setMensajes(prev => [...prev, msg]);\n    });\n    \n    return () => {\n      conexion.disconnect();\n    };\n  }, [roomId]);\n  \n  return (\n    <div>\n      {mensajes.map((msg, i) => (\n        <p key={i}>{msg.texto}</p>\n      ))}\n    </div>\n  );\n}\n\nCada vez que roomId cambia, se desconecta de la sala anterior y se conecta a la nueva.' },
    ]
  },
  {
    topicId: 'react-best-practices',
    title: 'Buenas Practicas en React',
    sections: [
      { heading: 'Reglas de oro', content: '1. **Nunca mutues el estado directamente**\n// MAL\nstate.items.push(newItem);\nsetState(state);\n\n// BIEN\nsetState([...state, newItem]);\n\n2. **No llames hooks dentro de condiciones**\n// MAL\nif (condicion) {\n  const [state, setState] = useState();\n}\n\n// BIEN\nconst [state, setState] = useState();\nif (condicion) { /* usar state */ }\n\n3. **Actualiza estado de forma funcional**\nsetCount(prev => prev + 1); // BIEN\nsetCount(count + 1); // Puede causar bugs' },
      { heading: 'Optimizacion', content: '**Evita renders innecesarios:**\n\n// Memoizacion de componentes\nconst MiComponente = React.memo(function MiComponente({ datos }) {\n  return <div>{datos.map(d => <p key={d.id}>{d.nombre}</p>)}</div>;\n});\n\n**Memoizacion de valores:**\nconst resultado = useMemo(() => {\n  return datos.filter(d => d.activo);\n}, [datos]);\n\n**Memoizacion de funciones:**\nconst handleClick = useCallback(() => {\n  console.log("Clic");\n}, []);\n\nNo memoices todo: solo lo costoso o lo que pasa como props.' },
      { heading: 'Organizacion del codigo', content: '**Estructura de carpetas:**\n\nsrc/\n  components/\n    Boton/\n      Boton.jsx\n      Boton.module.css\n  hooks/\n    useUsuarios.js\n  pages/\n    Home.jsx\n  services/\n    api.js\n  utils/\n    helpers.js\n\n**Reglas:**\n- Un componente por archivo.\n- Componentes relacionados juntos.\n- Hooks personalizados en hooks/.\n- Logica de API en services/.\n- Funciones utitarias en utils/.' },
      { heading: 'Manejo de errores', content: '**Error Boundaries:**\nclass ErrorBoundary extends React.Component {\n  state = { hasError: false };\n  \n  static getDerivedStateFromError(error) {\n    return { hasError: true };\n  }\n  \n  componentDidCatch(error, info) {\n    console.error(error, info);\n  }\n  \n  render() {\n    if (this.state.hasError) {\n      return <h1>Algo salio mal</h1>;\n    }\n    return this.props.children;\n  }\n}\n\n// Uso\n<ErrorBoundary>\n  <App />\n</ErrorBoundary>\n\n**Con hooks (patron):**\nfunction useErrorBoundary() {\n  const [error, setError] = useState(null);\n  \n  if (error) throw error;\n  \n  return { setError };\n}' },
    ]
  },
  {
    topicId: 'react-advanced-intro',
    title: 'Conceptos Avanzados',
    sections: [
      { heading: 'Custom Hooks', content: 'Un custom hook es una funcion que usa otros hooks y empieza con use:\n\nfunction useContador(valorInicial = 0) {\n  const [count, setCount] = useState(valorInicial);\n  \n  const incrementar = () => setCount(c => c + 1);\n  const decrementar = () => setCount(c => c - 1);\n  const resetear = () => setCount(valorInicial);\n  \n  return { count, incrementar, decrementar, resetear };\n}\n\n// Uso\nfunction App() {\n  const { count, incrementar, decrementar } = useContador(0);\n  return (\n    <div>\n      <p>{count}</p>\n      <button onClick={incrementar}>+</button>\n      <button onClick={decrementar}>-</button>\n    </div>\n  );\n}\n\nLos custom hooks comparten logica entre componentes sin duplicar codigo.' },
      { heading: 'Context API', content: 'Context permite pasar datos a traves del arbol sin props:\n\nconst TemaContext = createContext("claro");\n\nfunction App() {\n  const [tema, setTema] = useState("claro");\n  \n  return (\n    <TemaContext.Provider value={{ tema, setTema }}>\n      <Header />\n      <Main />\n    </TemaContext.Provider>\n  );\n}\n\nfunction Header() {\n  const { tema, setTema } = useContext(TemaContext);\n  return (\n    <button onClick={() => setTema(tema === "claro" ? "oscuro" : "claro")}>\n      Cambiar tema\n    </button>\n  );\n}\n\nUsalo para datos globales: tema, idioma, usuario autenticado.' },
      { heading: 'useRef', content: 'Referencia a elementos del DOM o valores mutables:\n\nfunction InputFoco() {\n  const inputRef = useRef(null);\n  \n  const enfocar = () => {\n    inputRef.current.focus();\n  };\n  \n  return (\n    <div>\n      <input ref={inputRef} type="text" />\n      <button onClick={enfocar}>Enfocar</button>\n    </div>\n  );\n}\n\n**useRef vs useState:**\n- useRef: Cambiar el valor NO re-renderiza.\n- useState: Cambiar el valor SI re-renderiza.\n\nUsalo para: acceder al DOM, guardar timers/intervalos, valores previos.' },
      { heading: 'Renderizado de listas avanzado', content: '**Listas con eventos:**\nfunction ListaTareas() {\n  const [tareas, setTareas] = useState([]);\n  \n  const eliminar = (id) => {\n    setTareas(prev => prev.filter(t => t.id !== id));\n  };\n  \n  const toggleCompletada = (id) => {\n    setTareas(prev => prev.map(t =>\n      t.id === id ? { ...t, completada: !t.completada } : t\n    ));\n  };\n  \n  return (\n    <ul>\n      {tareas.map(tarea => (\n        <li key={tarea.id}>\n          <span onClick={() => toggleCompletada(tarea.id)}>\n            {tarea.completada ? "DONE" : "TODO"} {tarea.texto}\n          </span>\n          <button onClick={() => eliminar(tarea.id)}>X</button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n\nNunca mutues el array: usa .map(), .filter(), .concat().' },
    ]
  },
];
