export interface Lesson {
  topicId: string;
  title: string;
  sections: { heading: string; content: string }[];
}

export const javascriptLessons: Lesson[] = [
  {
    topicId: 'js-fundamentals',
    title: 'Fundamentos y Sintaxis de JavaScript',
    sections: [
      { heading: 'Que es JavaScript?', content: 'JavaScript es un lenguaje de programacion que permite crear contenido dinamico en paginas web. A diferencia de HTML (estructura) y CSS (estilo), JavaScript agrega **interactividad**: responder a clics, validar formularios, animar elementos, enviar datos al servidor, etc. Es el lenguaje mas usado del mundo y funciona tanto en el navegador como en el servidor (Node.js).' },
      { heading: 'Variables: let, const y var', content: 'Las variables almacenan datos:\n\n**let** (recomendada para valores que cambian):\nlet edad = 25;\nedad = 26; // OK\n\n**const** (para valores que no cambian):\nconst PI = 3.14159;\n// PI = 3; // Error!\n\n**var** (antigua, evitar en codigo nuevo):\nvar nombre = "Ana";\n\nRegla: usa const por defecto. Cambia a let solo si necesitas reasignar.' },
      { heading: 'Tipos de datos', content: 'JavaScript tiene 8 tipos de datos:\n\n**Primitivos:**\n- **string**: Texto, ej: "Hola" o \'Mundo\'\n- **number**: Numeros, ej: 42, 3.14, Infinity, NaN\n- **boolean**: Verdadero/falso, ej: true, false\n- **undefined**: Sin valor asignado\n- **null**: Valor vacio intencional\n- **symbol**: Identificador unico\n- **bigint**: Numeros grandes, ej: 9007199254740991n\n\n**Referencia:**\n- **object**: Colecciones de datos, ej: {nombre: "Ana", edad: 25}\n- **array**: Listas, ej: [1, 2, 3] (es un objeto)' },
      { heading: 'Operadores basicos', content: '**Aritmeticos:**\n- **+** Suma, **-** Resta, ***** Multiplicacion, **/** Division\n- **%** Modulo (resto), **** Potencia\n\n**Comparacion:**\n- **==** Igual (con conversion): "5" == 5 es true\n- **===** Estricto (sin conversion): "5" === 5 es false\n- **!=** No igual, **!==** No igual estricto\n- **>**, **<**, **>=**, **<=**\n\n**Logicos:**\n- **&&** AND (ambos true)\n- **||** OR (al menos uno true)\n- **!** NOT (invierte)\n\nSiempre usa === y !== para evitar errores de tipo.' },
      { heading: 'Template literals', content: 'Forma moderna de crear strings con expresiones:\n\nEn tu archivo escribe:\nconst nombre = "Ana";\nconst edad = 25;\n\n// Sin template literals\nconst msg1 = "Hola " + nombre + ", tienes " + edad + " anios.";\n\n// Con template literals (usa la tecla de acento grave)\nconst msg2 = "Hola " + nombre + ", tienes " + edad + " anios.";\n\n// Para interpolaciones usa la sintaxis con dolar y llaves: ${variable}\n// Ejemplo: "Hola " + nombre + ", tienes " + edad + " anios."\n\n// Multilinea: el string puede ocupar varias lineas\nconst html = "<div><h1>" + nombre + "</h1><p>Edad: " + edad + "</p></div>";\n\nUsa la tecla de acento grave (junto a la 1) y ${} para interpolaciones.' },
    ]
  },
  {
    topicId: 'js-operators',
    title: 'Control de Flujo y Operadores',
    sections: [
      { heading: 'Condicionales', content: '**if/else:**\nconst edad = 18;\nif (edad >= 18) {\n  console.log("Mayor de edad");\n} else {\n  console.log("Menor de edad");\n}\n\n**else if:**\nconst nota = 85;\nif (nota >= 90) {\n  console.log("A");\n} else if (nota >= 80) {\n  console.log("B");\n} else {\n  console.log("C");\n}\n\n**Operador ternario** (atajo):\nconst resultado = edad >= 18 ? "Mayor" : "Menor";' },
      { heading: 'Switch', content: 'Util cuando hay muchas condiciones:\n\nconst dia = "lunes";\n\nswitch (dia) {\n  case "lunes":\n    console.log("Lunes de trabajo");\n    break;\n  case "martes":\n    console.log("Martes de trabajo");\n    break;\n  case "sabado":\n  case "domingo":\n    console.log("Fin de semana");\n    break;\n  default:\n    console.log("Otro dia");\n}\n\nImportante: no olvides break o ejecutara todos los cases.' },
      { heading: 'Bucles', content: '**for** (cuando sabes cuantas iteraciones):\nfor (let i = 0; i < 5; i++) {\n  console.log(i); // 0, 1, 2, 3, 4\n}\n\n**while** (cuando la condicion lo determina):\nlet i = 0;\nwhile (i < 5) {\n  console.log(i);\n  i++;\n}\n\n**do...while** (ejecuta al menos una vez):\nlet i = 0;\ndo {\n  console.log(i);\n  i++;\n} while (i < 5);\n\n**for...of** (recorre arrays):\nconst frutas = ["manzana", "pera", "uva"];\nfor (const fruta of frutas) {\n  console.log(fruta);\n}' },
      { heading: 'Break y Continue', content: '**break**: Sale del bucle completamente.\nfor (let i = 0; i < 10; i++) {\n  if (i === 5) break; // Sale en 5\n  console.log(i); // 0, 1, 2, 3, 4\n}\n\n**continue**: Salta a la siguiente iteracion.\nfor (let i = 0; i < 10; i++) {\n  if (i % 2 === 0) continue; // Salta pares\n  console.log(i); // 1, 3, 5, 7, 9\n}\n\nAmbos funcionan con cualquier tipo de bucle.' },
      { heading: 'Logica compuesta', content: 'Combina condiciones para logica compleja:\n\nconst edad = 25;\nconst tieneLicencia = true;\n\n// AND: ambas deben ser true\nif (edad >= 18 && tieneLicencia) {\n  console.log("Puede conducir");\n}\n\n// OR: al menos una debe ser true\nif (dia === "sabado" || dia === "domingo") {\n  console.log("Es fin de semana");\n}\n\n// NOT: invierte el valor\nif (!estaBaneado) {\n  console.log("Puede acceder");\n}\n\nOrden de precedencia: ! luego && luego ||' },
    ]
  },
  {
    topicId: 'js-functions',
    title: 'Funciones en JavaScript',
    sections: [
      { heading: 'Declaracion de funciones', content: 'Las funciones son bloques de codigo reutilizables:\n\n**Declaracion clasica:**\nfunction saludar(nombre) {\n  return "Hola, " + nombre + "!";\n}\nconsole.log(saludar("Ana")); // "Hola, Ana!"\n\n**Expresion de funcion:**\nconst sumar = function(a, b) {\n  return a + b;\n};\n\nLas declaraciones se "elevan" (hoisting): puedes llamarlas antes de declararlas. Las expresiones no.' },
      { heading: 'Arrow functions', content: 'Sintaxis moderna y concisa (ES6+):\n\n// Sintaxis completa\nconst sumar = (a, b) => {\n  return a + b;\n};\n\n// Sintaxis abreviada (una sola expresion)\nconst sumar2 = (a, b) => a + b;\n\n// Un solo parentesis (un parametro)\nconst duplicar = x => x * 2;\n\n// Sin parametros\nconst decirHola = () => "Hola!";\n\nVentaja principal: no crea su propio this (hereda el del contexto padre).' },
      { heading: 'Parametros y argumentos', content: '**Valores por defecto:**\nfunction saludar(nombre = "Visitante") {\n  return "Hola, " + nombre + "!";\n}\nsaludar(); // "Hola, Visitante!"\nsaludar("Ana"); // "Hola, Ana!"\n\n**Parametros rest (...args):**\nfunction sumar(...numeros) {\n  return numeros.reduce((total, n) => total + n, 0);\n}\nsumar(1, 2, 3, 4); // 10\n\n**Desestructuracion de objetos:**\nfunction mostrarUsuario({ nombre, edad }) {\n  console.log(nombre + " tiene " + edad + " anios");\n}\nmostrarUsuario({ nombre: "Ana", edad: 25, email: "a@b.com" });' },
      { heading: 'Funciones como callbacks', content: 'Una callback es una funcion que se pasa como argumento a otra:\n\nfunction procesar(datos, callback) {\n  const resultado = datos * 2;\n  callback(resultado);\n}\n\nprocesar(5, function(resultado) {\n  console.log(resultado); // 10\n});\n\nEjemplo practico con arrays:\nconst numeros = [1, 2, 3, 4, 5];\n\nconst dobles = numeros.map(n => n * 2);\n// [2, 4, 6, 8, 10]\n\nconst pares = numeros.filter(n => n % 2 === 0);\n// [2, 4]\n\nconst suma = numeros.reduce((acc, n) => acc + n, 0);\n// 15\n\nmap, filter y reduce son los metodos mas usados de arrays.' },
      { heading: 'Scope y closures', content: '**Scope (ambito):**\nlet global = "soy global";\n\nfunction miFuncion() {\n  let local = "soy local";\n  console.log(global); // OK\n  console.log(local); // OK\n}\n\nconsole.log(global); // OK\n// console.log(local); // Error! No existe fuera\n\n**Closure:** una funcion que "recuerda" su scope externo:\n\nfunction crearContador() {\n  let count = 0;\n  return function() {\n    count++;\n    return count;\n  };\n}\n\nconst contador = crearContador();\nconsole.log(contador()); // 1\nconsole.log(contador()); // 2\nconsole.log(contador()); // 3\n\ncount persiste porque el closure la mantiene viva.' },
    ]
  },
  {
    topicId: 'js-objects-arrays',
    title: 'Objetos y Arrays',
    sections: [
      { heading: 'Objetos literales', content: 'Los objetos almacenan datos como pares clave-valor:\n\nconst usuario = {\n  nombre: "Ana",\n  edad: 25,\n  activo: true,\n  direccion: {\n    ciudad: "CDMX",\n    pais: "Mexico"\n  }\n};\n\n// Acceder a propiedades\nconsole.log(usuario.nombre); // "Ana"\nconsole.log(usuario["edad"]); // 25\nconsole.log(usuario.direccion.ciudad); // "CDMX"\n\n// Agregar/modificar\nusuario.email = "ana@email.com";\nusuario.edad = 26;\n\n// Eliminar\ndelete usuario.activo;' },
      { heading: 'Desestructuracion de objetos', content: 'Extrae propiedades en variables de forma concisa:\n\nconst usuario = { nombre: "Ana", edad: 25, email: "a@b.com" };\n\n// Sin desestructuracion\nconst nombre = usuario.nombre;\nconst edad = usuario.edad;\n\n// Con desestructuracion\nconst { nombre2, edad2 } = usuario;\n// nombre2 = "Ana", edad2 = 25\n\n// Renombrar variables\nconst { nombre: userName } = usuario;\n// userName = "Ana"\n\n// Valor por defecto\nconst { telefono = "No registrado" } = usuario;\n// telefono = "No registrado"\n\n// Anidada\nconst { direccion: { ciudad } } = usuario;\n// ciudad = "CDMX"' },
      { heading: 'Metodos de objetos', content: 'Metodos utiles:\n\nconst usuario = { nombre: "Ana", edad: 25, email: "a@b.com" };\n\n// Object.keys() - Array de claves\nObject.keys(usuario); // ["nombre", "edad", "email"]\n\n// Object.values() - Array de valores\nObject.values(usuario); // ["Ana", 25, "a@b.com"]\n\n// Object.entries() - Array de pares [clave, valor]\nObject.entries(usuario);\n// [["nombre", "Ana"], ["edad", 25], ["email", "a@b.com"]]\n\n// Object.assign() - Copiar/fusionar\nconst copia = Object.assign({}, usuario, { edad: 26 });\n\n// Spread operator (moderno)\nconst copia2 = { ...usuario, edad: 26 };' },
      { heading: 'Arrays', content: 'Los arrays son listas ordenadas:\n\nconst frutas = ["manzana", "pera", "uva"];\n\n// Acceder\nfrutas[0]; // "manzana"\nfrutas.length; // 3\n\n// Agregar\nfrutas.push("naranja"); // Al final\nfrutas.unshift("fresa"); // Al inicio\n\n// Eliminar\nfrutas.pop(); // Del final\nfrutas.shift(); // Del inicio\nfrutas.splice(1, 1); // En posicion 1, eliminar 1\n\n// Metodos de transformacion\nfrutas.sort(); // Ordenar alfabeticamente\nfrutas.reverse(); // Invertir orden\nfrutas.includes("uva"); // true\nfrutas.indexOf("pera"); // 1' },
      { heading: 'Metodos de arrays funcionales', content: 'Los metodos funcionales no mutan el array original:\n\nconst nums = [1, 2, 3, 4, 5];\n\n// map: transforma cada elemento\nconst dobles = nums.map(n => n * 2);\n// [2, 4, 6, 8, 10]\n\n// filter: selecciona elementos\nconst pares = nums.filter(n => n % 2 === 0);\n// [2, 4]\n\n// reduce: acumula en un solo valor\nconst suma = nums.reduce((acc, n) => acc + n, 0);\n// 15\n\n// find: primer elemento que cumple\nconst mayor = nums.find(n => n > 3);\n// 4\n\n// some / every: verificacion\nnums.some(n => n > 3); // true\nnums.every(n => n > 0); // true\n\nEstos metodos se encadenan: nums.filter(...).map(...).reduce(...)' },
      { heading: 'Copia profunda vs superficial', content: '**Copia superficial** (solo primer nivel):\nconst original = { a: 1, b: { c: 2 } };\nconst copia = { ...original };\ncopia.b.c = 99;\nconsole.log(original.b.c); // 99! (modificado)\n\n**Copia profunda** (todos los niveles):\n// Opcion 1: structuredClone (moderno)\nconst copia2 = structuredClone(original);\n\n// Opcion 2: JSON (limitaciones)\nconst copia3 = JSON.parse(JSON.stringify(original));\n\n// Opcion 3: Spread + recursion\nfunction deepCopy(obj) {\n  if (obj === null || typeof obj !== "object") return obj;\n  const copy = Array.isArray(obj) ? [] : {};\n  for (const key in obj) {\n    copy[key] = deepCopy(obj[key]);\n  }\n  return copy;\n}\n\nUsa structuredClone() para la mayoria de casos.' },
    ]
  },
  {
    topicId: 'js-dom-events',
    title: 'DOM y Eventos',
    sections: [
      { heading: 'Que es el DOM?', content: 'El DOM (Document Object Model) es una representacion en memoria del documento HTML. JavaScript puede leer y modificar el DOM para cambiar la pagina dinamicamente.\n\n// Obtener elementos\nconst titulo = document.getElementById("titulo");\nconst items = document.querySelectorAll(".item");\nconst primero = document.querySelector("h1");\n\n// Modificar contenido\ntitulo.textContent = "Nuevo titulo";\ntitulo.innerHTML = "<em>Titulo en cursiva</em>";\n\n// Modificar estilos\ntitulo.style.color = "red";\ntitulo.classList.add("activo");\ntitulo.classList.toggle("visible");' },
      { heading: 'Crear y eliminar elementos', content: '// Crear elemento\nconst nuevoDiv = document.createElement("div");\nnuevoDiv.textContent = "Soy nuevo";\nnuevoDiv.classList.add("card");\n\n// Agregar al DOM\ndocument.body.appendChild(nuevoDiv); // Al final del body\n\n// Insertar en posicion especifica\nconst referencia = document.getElementById("existente");\nreferencia.before(nuevoDiv); // Antes\nreferencia.after(nuevoDiv); // Despues\nreferencia.prepend(nuevoDiv); // Como primer hijo\nreferencia.appendChild(nuevoDiv); // Como ultimo hijo\n\n// Eliminar\ndocument.body.removeChild(nuevoDiv);\n// O moderno:\nnuevoDiv.remove();' },
      { heading: 'Eventos', content: 'Los eventos responden a acciones del usuario:\n\nconst boton = document.getElementById("miBoton");\n\n// Metodo 1: addEventListener (recomendado)\nboton.addEventListener("click", function(event) {\n  console.log("Clic en:", event.target);\n});\n\n// Metodo 2: onclick (antiguo)\nboton.onclick = function() {\n  console.log("Clic!");\n};\n\nEventos comunes:\n- **click**: Clic del mouse\n- **dblclick**: Doble clic\n- **mouseover**/**mouseout**: Mouse entra/sale\n- **keydown**/**keyup**: Tecla presionada/soltada\n- **input**: Cambio en input\n- **submit**: Envio de formulario\n- **load**: Pagina cargada\n- **scroll**: Desplazamiento' },
      { heading: 'Event delegation', content: 'En lugar de agregar un evento a cada elemento hijo, agrega uno al padre:\n\n// Mal: eventos individuales\nconst items = document.querySelectorAll(".item");\nitems.forEach(item => {\n  item.addEventListener("click", handleClick);\n});\n\n// Bien: delegacion\ndocument.getElementById("lista").addEventListener("click", function(e) {\n  if (e.target.matches(".item")) {\n    handleClick(e);\n  }\n});\n\nVentajas:\n- Menos memoria usada.\n- Funciona con elementos creados dinamicamente.\n- Codigo mas limpio.' },
      { heading: 'Formularios con JavaScript', content: 'const form = document.getElementById("miForm");\n\nform.addEventListener("submit", function(e) {\n  e.preventDefault(); // Evita recarga\n  \n  const nombre = form.elements.nombre.value;\n  const email = form.elements.email.value;\n  \n  if (!nombre || !email) {\n    alert("Completa todos los campos");\n    return;\n  }\n  \n  console.log("Enviando:", { nombre, email });\n});\n\n// Validacion en tiempo real\nconst input = document.getElementById("email");\ninput.addEventListener("input", function() {\n  const isValid = input.value.includes("@");\n  input.style.borderColor = isValid ? "green" : "red";\n});' },
    ]
  },
  {
    topicId: 'js-async',
    title: 'Asincronia en JavaScript',
    sections: [
      { heading: 'Por que asincronia?', content: 'JavaScript es **single-threaded** (un solo hilo). Sin asincronia, una peticion HTTP de 3 segundos bloquearia toda la pagina.\n\nLa asincronia permite:\n- Hacer peticiones de red sin bloquear.\n- Cargar archivos en segundo plano.\n- Ejecutar codigo despues de un tiempo.\n\n**Callbacks** (forma antigua):\nsetTimeout(function() {\n  console.log("Despues de 2 segundos");\n}, 2000);' },
      { heading: 'Promesas', content: 'Una promesa representa un valor que estara disponible en el futuro:\n\nconst miPromesa = new Promise((resolve, reject) => {\n  const exito = true;\n  if (exito) {\n    resolve("Datos recibidos");\n  } else {\n    reject("Error de red");\n  }\n});\n\nmiPromesa\n  .then(datos => console.log(datos))\n  .catch(error => console.error(error))\n  .finally(() => console.log("Siempre se ejecuta"));\n\nEstados:\n- **pending**: Aun no se resolvio.\n- **fulfilled**: Se completo exitosamente.\n- **rejected**: Hubo un error.' },
      { heading: 'Async/Await', content: 'Syntactic sugar sobre promesas, mas legible:\n\nasync function obtenerDatos() {\n  try {\n    const respuesta = await fetch("https://api.ejemplo.com/datos");\n    const datos = await respuesta.json();\n    console.log(datos);\n  } catch (error) {\n    console.error("Error:", error);\n  }\n}\n\nobtenerDatos();\n\nawait solo funciona dentro de funciones async.\n\n**Ventajas sobre .then():**\n- Codigo mas lineal y legible.\n- Mas facil de depurar.\n- Mejor manejo de errores con try/catch.' },
      { heading: 'Fetch API', content: 'Fetch es la API moderna para peticiones HTTP:\n\n// GET\nconst respuesta = await fetch("https://api.ejemplo.com/users");\nconst usuarios = await respuesta.json();\n\n// POST\nconst nuevaResp = await fetch("https://api.ejemplo.com/users", {\n  method: "POST",\n  headers: { "Content-Type": "application/json" },\n  body: JSON.stringify({ nombre: "Ana", edad: 25 })\n});\n\n// Verificar status\nif (!respuesta.ok) {\n  throw new Error("HTTP " + respuesta.status);\n}\n\nFetch no lanza error en respuestas HTTP 4xx/5xx, solo en errores de red.' },
      { heading: 'Patrones asincronos', content: '**Ejecutar en paralelo:**\nconst [usuarios, posts] = await Promise.all([\n  fetch("/api/users").then(r => r.json()),\n  fetch("/api/posts").then(r => r.json())\n]);\n\n**Primero que termine:**\nconst resultado = await Promise.race([\n  fetch("/api/slow"),\n  fetch("/api/fast")\n]);\n\n**Manejo de errores parcial:**\nconst resultados = await Promise.allSettled([\n  fetch("/api/ok"),\n  fetch("/api/fail")\n]);\nresultados.forEach(r => {\n  if (r.status === "fulfilled") console.log(r.value);\n  else console.error(r.reason);\n});' },
    ]
  },
  {
    topicId: 'js-extras',
    title: 'Extras y Buenas Practicas',
    sections: [
      { heading: 'JSON', content: 'JSON (JavaScript Object Notation) es el formato estandar para intercambiar datos:\n\n// Objeto a JSON string\nconst usuario = { nombre: "Ana", edad: 25 };\nconst json = JSON.stringify(usuario);\n// \'{"nombre":"Ana","edad":25}\'\n\n// JSON string a objeto\nconst obj = JSON.parse(json);\n// { nombre: "Ana", edad: 25 }\n\n// Con espaciado\nJSON.stringify(usuario, null, 2);\n\nUsos: APIs, almacenamiento en localStorage, configuracion.' },
      { heading: 'Desestructuracion avanzada', content: '**Arrays:**\nconst [primero, segundo, ...resto] = [1, 2, 3, 4, 5];\n// primero=1, segundo=2, resto=[3,4,5]\n\n// Ignorar elementos\nconst [,, tercero] = [1, 2, 3];\n// tercero=3\n\n**Parametros:**\nfunction configurar({\n  host = "localhost",\n  port = 3000,\n  verbose = false\n} = {}) {\n  console.log(host + ":" + port);\n}\nconfigurar({ port: 8080 });' },
      { heading: 'Optional chaining y Nullish coalescing', content: '**Optional chaining (?.):**\nconst usuario = { direccion: { ciudad: "CDMX" } };\n\n// Sin optional chaining\nconst ciudad = usuario && usuario.direccion && usuario.direccion.ciudad;\n\n// Con optional chaining\nconst ciudad2 = usuario?.direccion?.ciudad; // "CDMX"\nconst telefono = usuario?.telefono; // undefined (sin error)\nconst zip = usuario?.direccion?.zip?.code; // undefined\n\n**Nullish coalescing (??):**\nconst valor = null ?? "default"; // "default"\nconst valor2 = 0 ?? "default"; // 0 (no es null/undefined)\nconst valor3 = "" ?? "default"; // "" (no es null/undefined)\n\n?? solo reemplaza null/undefined, a diferencia de || que reemplaza cualquier falsy.' },
      { heading: 'Modulos ES6', content: 'Organiza tu codigo en modulos separados:\n\n**Archivo: utils.js**\nexport function sumar(a, b) {\n  return a + b;\n}\n\nexport const PI = 3.14159;\n\nexport default function saludar(nombre) {\n  return "Hola " + nombre;\n}\n\n**Archivo: main.js**\nimport saludar, { sumar, PI } from "./utils.js";\n\nconsole.log(saludar("Ana"));\nconsole.log(sumar(1, 2));\nconsole.log(PI);\n\n- **export default**: Exportacion principal (una por archivo).\n- **export**: Exportaciones nombradas (multiples).\n- **import**: Importa lo que necesitas.' },
      { heading: 'Buenas practicas', content: '1. **Usa const por defecto**, let cuando reasignes, nunca var.\n2. **Usa ===** en lugar de ==.\n3. **Usa arrow functions** para callbacks.\n4. **Usa template literals** en lugar de concatenacion.\n5. **Usa desestructuracion** para objetos y arrays.\n6. **Usa async/await** en lugar de .then() anidados.\n7. **Usa optional chaining** (?.) para accesos seguros.\n8. **No mutes arrays**: usa map, filter, reduce.\n9. **Maneja errores** con try/catch en codigo asincrono.\n10. **Organiza en modulos** en lugar de archivos gigantes.' },
    ]
  },
];
